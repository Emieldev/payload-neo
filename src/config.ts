const config = {
    PREFIX: "pls ",
    allowedID: ["176457969465163776"],

    files: {
        LOGS_CSS: __dirname + "/assets/dark-logs.css",
    },

    GOOGLE_CREDENTIALS_PATH: __dirname + "../googledata.json",
}
export default config;
